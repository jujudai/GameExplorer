<h1><%= @user.name %></h1>
<p>Email: <%= @user.email %></p>

<% unless @user.guest_user? %>
  <div class="row">
    <%= link_to 'プロフィール編集', edit_user_registration_path, class: "btn btn-outline-secondary btn-block fas fa-user-cog" %>
  </div>
<%= link_to '退会する', user_registration_path, method: :delete, data: { confirm: '本当に退会しますか？' }, class: 'btn btn-danger' %>
<% end %>